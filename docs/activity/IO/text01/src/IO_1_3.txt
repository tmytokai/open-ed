// デジタル入力初期化
void init_input(void)
{
    // データシート 126p を参考にして RB0 を入力モードにしてください
    TRISB = TRISB | 0b? ;

    // ポートBの(弱)プルアップ抵抗の設定です
    // 詳しくはデータシート 126p を参照してください
    // 今回は RB0 のプルアップ抵抗を有効にしてください
    WPUB  = WPUB  | 0b? ;

    // PICの内部(弱)プルアップ抵抗機能を ON にする設定です
    // ローアクティブなので 0 をセットすると ON になりす
    // 内部プルアップ抵抗機能を使いたい場合は忘れず ON にしてください
    // 詳しくはデータシート 175p を参照してください
    OPTION_REGbits.nWPUEN = ? ;
}

void main(void) {

    init();
    init_input();

    while(1){
        if( RB0 == 0 ) RA0 = ? ;
        else RA0 = ? ;
    }
    
    return;
}
