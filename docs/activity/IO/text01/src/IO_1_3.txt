#include "config.h"
#include "setting.h"

void main(void) {

    init();

    while(1){
        if( RB0 == ? ){
            RA0 = 0;
            NOP(); // 何もしないという命令
        }
        else RA0 = 1;
    }
    
    return;
}
