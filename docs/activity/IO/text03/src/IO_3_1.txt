#include "setting.h"

void main(void){

    init();
    init_uart();
    init_adc();
  
    while(1){
      int result = adc( ? );
      printf("ADC = %d\r\n",result);
      __delay_ms(1000);
    }
    
    return;
}
